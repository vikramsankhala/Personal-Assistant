# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
aiosqlite>=0.19.0
psycopg2-binary>=2.9.9

# Task Queue
redis>=5.0.0
celery[redis]>=5.3.0

# API Client for Hugging Face
httpx>=0.26.0

# ASR & ML (Lightweight - use existing transcription services)
faster-whisper>=1.0.0
pyannote.audio>=3.1.0
# Heavy dependencies removed: torch, torchaudio
# These can be kept if transcription needs to be done locally
# Otherwise use external APIs

# PII & Privacy  
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# Utilities
pydantic>=2.5.0
pydantic-settings>=2.1.0
websockets>=12.0
aiofiles>=23.2.0
python-magic>=0.4.27

# Optional: Document export
python-docx>=1.0.0
reportlab>=4.0.0